# Supabase project used for the global leaderboard + cloud profile sync.
# These are the public (RLS-protected) URL and publishable/anon key — safe to
# expose in a client bundle. Copy this file to .env and fill in your own
# project if you want to point the game at a different Supabase project;
# otherwise the app falls back to the bundled defaults in src/engine/Cloud.ts.
VITE_SUPABASE_URL=https://gvpnrasaurhncwimdemw.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_znmztYzSn9MjcmMs5oV3vQ_qofm8593
